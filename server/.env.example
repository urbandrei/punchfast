# Database Configuration
# Option 1: Use DATABASE_URL for remote database (Render, Codespaces)
# This is the recommended approach for connecting to remote databases
DATABASE_URL=postgresql://user:password@host:5432/database

# Option 2: Use individual vars for local PostgreSQL database
# Uncomment these if you want to use a local PostgreSQL instance
# POSTGRES_DB=punchfast_dev
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=password
# DB_HOST=localhost

# Server Configuration
PORT=5000
NODE_ENV=development
