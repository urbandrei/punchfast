services:
  - type: web
    name: punchfast
    env: node
    region: oregon
    plan: free
    buildCommand: chmod +x run.sh && cd client && npm install && npm run build && cd ../server && npm install
    startCommand: cd server && NODE_ENV=production node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: punchfast-db
          property: connectionString
      - key: ADMIN_TOKEN
        sync: false
    healthCheckPath: /api

databases:
  - name: punchfast-db
    region: oregon
    plan: free
    databaseName: punchfast
    user: punchfast_user
